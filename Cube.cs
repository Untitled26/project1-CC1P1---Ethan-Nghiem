using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cube : MonoBehaviour
{
    string var4 = "Gaming";
        string var5 = "is";
    string var6 = "Life";

    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(var4 + var5 + var6);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
